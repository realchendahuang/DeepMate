[package]
name = "deepmate-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
thiserror.workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"

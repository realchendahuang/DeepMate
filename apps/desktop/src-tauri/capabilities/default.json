{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main DeepMate window: the core IPC surface plus the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-app-commands"]
}
